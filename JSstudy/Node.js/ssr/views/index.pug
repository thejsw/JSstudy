

extends layout

block content
  h1= title
  p Welcome to #{title}
  p about #{title}

  a(href="/create") New Post 

  ul
    each post in posts
      li
        a(href=`/p/${post}`)= post.split('.')[0]
